<?xml version="1.0" encoding="utf-8"?>
<!--
    Robustes Standard Widget Layout (4x2)
    Nutzt nur LinearLayout für maximale Kompatibilität.
    Keine Elevation im Root-Element.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background_modern"
    android:padding="16dp">

    <!-- Header: Status + Titel -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <!-- Status Punkt -->
        <View
            android:id="@+id/widget_status_indicator"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/widget_status_inactive"
            android:layout_marginEnd="12dp" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Arbeitszeit"
            android:textSize="14sp"
            android:textColor="#49454F"
            android:textStyle="normal" />
    </LinearLayout>

    <!-- Hauptanzeige: Dauer -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:paddingBottom="16dp">

        <!-- Statische Dauer (wenn gestoppt) -->
        <TextView
            android:id="@+id/widget_duration_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0:00h"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="#6750A4"
            android:visibility="visible" />

        <!-- Chronometer (wenn läuft) -->
        <Chronometer
            android:id="@+id/widget_chronometer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="#6750A4"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Info Zeile: Start / Ende -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Start: "
            android:textColor="#79747E"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/widget_start_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--:--"
            android:textColor="#1C1B1F"
            android:textStyle="bold"
            android:textSize="14sp" />

        <!-- Spacer -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ende: "
            android:textColor="#79747E"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/widget_end_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--:--"
            android:textColor="#1C1B1F"
            android:textStyle="bold"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Button Leiste -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="bottom">

        <Button
            android:id="@+id/widget_btn_start"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="START"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="@drawable/widget_button_success"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/widget_btn_pause"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="PAUSE"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#6750A4"
            android:background="@drawable/widget_button_secondary"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/widget_btn_stop"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="STOP"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="@drawable/widget_button_primary" />
    </LinearLayout>

</LinearLayout>
