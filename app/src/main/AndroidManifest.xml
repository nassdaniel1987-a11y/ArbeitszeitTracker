<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- OSMDroid Map Berechtigungen -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Geofencing Berechtigungen -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".TrackerApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArbeitszeitTracker">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ArbeitszeitTracker"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Zeit-Stempel Widget Klein (3x1) -->
        <receiver
            android:name="com.arbeitszeit.tracker.widget.TimeStampWidgetSmall"
            android:exported="true"
            android:label="@string/widget_small_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.arbeitszeit.tracker.ACTION_QUICK_STAMP_SMALL" />
                <action android:name="com.arbeitszeit.tracker.ACTION_START_SMALL" />
                <action android:name="com.arbeitszeit.tracker.ACTION_PAUSE_SMALL" />
                <action android:name="com.arbeitszeit.tracker.ACTION_REFRESH_SMALL" />
                <action android:name="com.arbeitszeit.tracker.ACTION_MIDNIGHT_RESET_SMALL" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_time_stamp_small_info" />
        </receiver>

        <!-- Schnell-Stempel Widget (1x1) -->
        <receiver
            android:name="com.arbeitszeit.tracker.widget.QuickStampWidget"
            android:exported="true"
            android:label="@string/widget_quick_stamp_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.arbeitszeit.tracker.ACTION_QUICK_STAMP" />
                <action android:name="com.arbeitszeit.tracker.ACTION_REFRESH_QUICK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_stamp_info" />
        </receiver>

        <!-- Statistik-Widget (4x2) -->
        <receiver
            android:name="com.arbeitszeit.tracker.widget.StatistikWidget"
            android:exported="true"
            android:label="@string/widget_statistik_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.arbeitszeit.tracker.ACTION_REFRESH_STATISTIK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_statistik_info" />
        </receiver>


        <!-- Geofencing Receiver -->
        <receiver
            android:name=".geofencing.GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Notification Action Receiver -->
        <receiver
            android:name=".notification.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.arbeitszeit.tracker.ACTION_STAMP_START" />
                <action android:name="com.arbeitszeit.tracker.ACTION_STAMP_END" />
                <action android:name="com.arbeitszeit.tracker.ACTION_DISMISS" />
            </intent-filter>
        </receiver>

        <!-- Auto-Start Receiver (Legacy) -->
        <receiver
            android:name=".autostart.AutoStartReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.arbeitszeit.tracker.AUTO_START_REMINDER" />
                <action android:name="com.arbeitszeit.tracker.AUTO_START" />
            </intent-filter>
        </receiver>

        <!-- Auto-Start Alarm Receiver (Neu - AlarmManager basiert) -->
        <receiver
            android:name=".worker.AutoStartAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.arbeitszeit.tracker.AUTO_START_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Reminder Alarm Receiver -->
        <receiver
            android:name=".worker.ReminderAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.arbeitszeit.tracker.REMINDER_MORNING" />
                <action android:name="com.arbeitszeit.tracker.REMINDER_EVENING" />
                <action android:name="com.arbeitszeit.tracker.REMINDER_MISSING" />
            </intent-filter>
        </receiver>

        <!-- Boot Completed Receiver - Stellt Auto-Start und Reminder-Alarme nach Neustart wieder her -->
        <receiver
            android:name=".worker.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider für Share-Funktion (Excel-Export) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>